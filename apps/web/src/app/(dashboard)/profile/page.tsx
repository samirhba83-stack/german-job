import { NotYetAvailable } from '@/components/shell/not-yet-available';

export default function ProfilePage() {
  return (
    <NotYetAvailable
      title="Profile"
      reason="The profiles backend is real and live, but the Profile screen is reserved for a future milestone — it isn't built yet."
    />
  );
}
