/** Own copy, distinct from Applications' ActorRole — the Campaign module stays self-contained. */
export enum CampaignActorRole {
  CANDIDATE = 'CANDIDATE',
  ADMIN = 'ADMIN',
  SYSTEM = 'SYSTEM',
  AUTOMATION = 'AUTOMATION',
}
